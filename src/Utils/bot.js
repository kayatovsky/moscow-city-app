import {BOT_TOKEN, TG_GROUP_ID} from "../config";

export const sendMessage = message => {
    /*fetch(`https://api.telegram.org/bot${BOT_TOKEN}/sendMessage?chat_id=${TG_GROUP_ID}&text=${message}`, {
        method: 'POST'
    })*/
}
